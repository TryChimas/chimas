language: python
python:
  - "3.5"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script:
        - py.test
        - PYTHONPATH="." coverage run tests/test_basic.py
after_success:
        - coveralls
